import{useState,useEffect}from"react";import useIsBrowser from"../useIsBrowser";export default function useLocalStorage<T>(key:string,initialValue:T){const isBrowser=useIsBrowser();const[storedValue,setStoredValue]=useState<T>(()=>{if(!isBrowser){return initialValue;}const item=localStorage.getItem(key);return item?JSON.parse(item):initialValue});useEffect(()=>{if(isBrowser){localStorage.setItem(key,JSON.stringify(storedValue))}},[key,storedValue,isBrowser]);return[storedValue,setStoredValue]as const}